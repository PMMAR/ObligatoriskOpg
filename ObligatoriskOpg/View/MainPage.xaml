<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ObligatoriskOpg"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ViewModel="using:ObligatoriskOpg.ViewModel"
    x:Class="ObligatoriskOpg.MainPage"
    mc:Ignorable="d">

    <Page.DataContext>
        <ViewModel:GuestViewModel/>
    </Page.DataContext>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="43*"/>
            <RowDefinition Height="77*"/>
        </Grid.RowDefinitions>
        <ListView x:Name="listView" HorizontalAlignment="Left" Margin="222,10,0,10" Width="177" ItemsSource="{Binding gs.Guests}" Grid.RowSpan="2" BorderBrush="Black" BorderThickness="5" Background="#FFC3C3C3"/>
    </Grid>
</Page>
